# Dependencies
node_modules/

# This project uses Yarn classic — yarn.lock is the source of truth.
# Keep npm's lockfile out so CF Pages (and anyone running `npm install`)
# doesn't generate a parallel, drifting lockfile.
package-lock.json

# Build output
dist/
dist-ssr/
dist-tsbuildinfo/
*.tsbuildinfo

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# OS
Thumbs.db

# Claude Code local settings & scratch
.claude/settings.local.json
.claude/scheduled_tasks.lock
